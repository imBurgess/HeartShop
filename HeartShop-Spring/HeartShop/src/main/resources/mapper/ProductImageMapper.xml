<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.HeartShop.mapper.ProductImageMapper">

    <resultMap id="BaseResultMap" type="com.HeartShop.entity.ProductImage">
        <id property="imageId" column="image_id"/>
        <result property="productId" column="product_id"/>
        <result property="imageUrl" column="image_url"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="imageId">
        INSERT INTO product_image (product_id, image_url, sort_order)
        VALUES
        <foreach collection="images" item="img" separator=",">
            (#{img.productId}, #{img.imageUrl}, #{img.sortOrder})
        </foreach>
    </insert>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT * FROM product_image
        WHERE product_id = #{productId}
        ORDER BY sort_order ASC, created_at DESC
    </select>

    <delete id="deleteByProductId">
        DELETE FROM product_image WHERE product_id = #{productId}
    </delete>

</mapper>
